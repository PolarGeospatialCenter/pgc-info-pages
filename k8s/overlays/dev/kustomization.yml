apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

bases:
  - ../../base

namespace: info-pages-dev
nameSuffix: -dev

commonLabels:
  app: info-pages-dev

patchesStrategicMerge:
  - deployment_image.yml
